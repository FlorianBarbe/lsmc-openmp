/**
 * Implémentation du module RNG (Random Number Generator)
 * ------------------------------------------------------
 * Fournit un générateur de nombres aléatoires suivant une loi normale N(0,1).
 */

#include "rng.hpp"

 // Rien d'autre à implémenter ici pour le moment :
 // toute la logique est déjà dans le header (rng.hpp),
 // car les fonctions sont courtes et inline-friendly.
 //
 // Si tu veux plus tard ajouter :
 //  - une graine contrôlable,
 //  - ou des lois supplémentaires (uniforme, exponentielle),
 //  tu pourras développer leur implémentation ici.
